version: '2'
services:
    nginx:
        image: 'nginx'
        volumes:
            - './build:/usr/share/nginx/html:ro'
        ports:
            - '8100:80'
    webpack:
        build: '.'
        command: ['webpack', '--progress', '--colors']
        volumes:
            - './src:/app/src'
            - './webpack.config.js:/app/webpack.config.js'
            - './assets:/app/assets'
            - './plugins:/app/plugins'
            - './build:/app/build'
